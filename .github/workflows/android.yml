name: Android CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

env:
  JAVA_VERSION: "21"
  RUST_TOOLCHAIN: "stable"
  SHARED_RUST_REPOSITORY: "neboman11/any-player-shared-rust"
  ANDROID_NDK_VERSION: "27.0.12077973"
  GRADLE_OPTS: "-Dorg.gradle.warning.mode=summary"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout app repository
        uses: actions/checkout@v4

      - name: Checkout shared Rust workspace
        uses: actions/checkout@v4
        with:
          repository: ${{ env.SHARED_RUST_REPOSITORY }}
          token: ${{ secrets.CI_REPO_READ_TOKEN != '' && secrets.CI_REPO_READ_TOKEN || github.token }}
          path: any-player-shared-rust

      - name: Link shared Rust workspace path
        run: ln -sfn "$GITHUB_WORKSPACE/any-player-shared-rust" "$GITHUB_WORKSPACE/../any-player-shared-rust"

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_TOOLCHAIN }}
          targets: aarch64-linux-android,x86_64-linux-android,armv7-linux-androideabi,i686-linux-android

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: gradle

      - name: Verify Java toolchain
        run: |
          java -version

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android NDK
        run: |
          SDK_ROOT="${ANDROID_SDK_ROOT:-${ANDROID_HOME:-$HOME/Android/Sdk}}"
          if [ -z "$SDK_ROOT" ] || [ ! -d "$SDK_ROOT" ]; then
            echo "Resolved Android SDK root is invalid: '$SDK_ROOT'"
            exit 1
          fi
          yes | sdkmanager --sdk_root="$SDK_ROOT" "ndk;${ANDROID_NDK_VERSION}"
          if [ ! -d "$SDK_ROOT/ndk/${ANDROID_NDK_VERSION}" ]; then
            echo "Expected NDK path not found: $SDK_ROOT/ndk/${ANDROID_NDK_VERSION}"
            exit 1
          fi
          echo "ANDROID_NDK_HOME=$SDK_ROOT/ndk/${ANDROID_NDK_VERSION}" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$SDK_ROOT" >> "$GITHUB_ENV"
          echo "ANDROID_HOME=$SDK_ROOT" >> "$GITHUB_ENV"

      - name: Generate local.properties
        run: |
          SDK_ROOT="${ANDROID_SDK_ROOT:-${ANDROID_HOME:-$HOME/Android/Sdk}}"
          if [ -z "$SDK_ROOT" ] || [ ! -d "$SDK_ROOT" ]; then
            echo "Resolved Android SDK root is invalid: '$SDK_ROOT'"
            exit 1
          fi
          echo "sdk.dir=$SDK_ROOT" > local.properties
          echo "spotifyClientId=ci-placeholder-client-id" >> local.properties

      - name: Grant execute permission
        run: chmod +x gradlew

      - name: Assemble debug
        run: ./gradlew --no-daemon --warning-mode=summary :app:assembleDebug -PandroidNdkDir="$ANDROID_NDK_HOME"

      - name: Unit tests
        run: ./gradlew --no-daemon --warning-mode=summary :app:testDebugUnitTest -PandroidNdkDir="$ANDROID_NDK_HOME"

      - name: Lint
        run: ./gradlew --no-daemon --warning-mode=summary :app:lintDebug -PandroidNdkDir="$ANDROID_NDK_HOME"
